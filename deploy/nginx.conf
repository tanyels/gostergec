upstream fonako_app {
    server fonako:3000;
}

# Redirect HTTP to HTTPS (enabled after SSL cert is obtained)
# server {
#     listen 80;
#     server_name fonako.com www.fonako.com;
#
#     location /.well-known/acme-challenge/ {
#         root /var/www/certbot;
#     }
#
#     location / {
#         return 301 https://$host$request_uri;
#     }
# }

# HTTP server (initial, before SSL)
server {
    listen 80;
    server_name fonako.com www.fonako.com;

    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }

    location / {
        proxy_pass http://fonako_app;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;
    }
}

# HTTPS server (uncomment after obtaining SSL cert, comment out HTTP block above)
# server {
#     listen 443 ssl;
#     server_name fonako.com www.fonako.com;
#
#     ssl_certificate /etc/letsencrypt/live/fonako.com/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/fonako.com/privkey.pem;
#     ssl_protocols TLSv1.2 TLSv1.3;
#     ssl_ciphers HIGH:!aNULL:!MD5;
#
#     location / {
#         proxy_pass http://fonako_app;
#         proxy_http_version 1.1;
#         proxy_set_header Upgrade $http_upgrade;
#         proxy_set_header Connection 'upgrade';
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#         proxy_cache_bypass $http_upgrade;
#     }
# }
